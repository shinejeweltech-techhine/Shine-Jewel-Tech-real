// API Disabled as per request
// This file is kept as a placeholder if you wish to re-enable AI features in the future.
// To re-enable, you will need to:
// 1. Add @google/genai back to index.html importmap
// 2. Uncomment the code below

export const getMachineRecommendation = async (userQuery: string): Promise<string> => {
  return "AI features are currently disabled.";
};
